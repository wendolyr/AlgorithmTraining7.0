#include <algorithm>
#include <iostream>
#include <vector>

#define TYPE 30
using namespace std;

int main() {
  long long m = 0;
  cin >> m;

  vector<long long> a(31);
  for (int i = 0; i <= TYPE; i++) {
    cin >> a[i];
  }

    return 0;
}