#include <iostream>

using namespace std;

void set_bit(int x, int y, int z) {}

int main() {
  int n = 0, k = 0;
  cin >> n >> k;

  int size = (k + 31) >> 5;
  vector<vector<vector<unsigned int>>> board(
      size, vector<vector<unsigned int>>(size, vector<unsigned int>(size, 0)));

  for (int) }